Read the HTML file at /root/input.html and extract the plain text of the body content. Strip all HTML tags, scripts, and styles. Concatenate the visible text from the body (in document order), with single spaces between adjacent text nodes, and write the result as JSON to /root/result.json in the form {"body_text": "..."}. The body_text should be a single string containing all visible body text, with whitespace collapsed to single spaces and trimmed.
